#Abstraction is the process of hiding the internal implementation and showing
# only the essential features of an object.


"""from abc import ABC, abstractmethod
class parent(ABC):
    @abstractmethod
    def fun(self):
        pass
class Child(parent):
    def Display(self):
        print("hello")
    def fun(self):
            print("asdfghjkl")
ob=Child()
ob.Display()
ob.fun()"""